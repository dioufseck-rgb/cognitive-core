# Cognitive Core — Dependencies
#
# Core is always required. Install ONE provider package for your platform.
#
# Quick start:
#   pip install -r requirements.txt                          # core only
#   pip install -r requirements.txt langchain-azure-ai azure-identity  # Azure AI Foundry (recommended)
#   pip install -r requirements.txt langchain-openai         # Azure OpenAI / OpenAI direct
#   pip install -r requirements.txt langchain-google-genai   # Google Gemini
#   pip install -r requirements.txt langchain-aws            # Amazon Bedrock

# --- Core (always required) ---
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
pyyaml>=6.0

# --- LLM Providers (install one or more) ---
# langchain-azure-ai>=0.1.0        # Azure AI Foundry (Model Catalog + Azure OpenAI)
# azure-identity>=1.17.0           # Azure credential management (for Foundry)
# langchain-openai>=0.3.0          # Azure OpenAI Service / OpenAI direct
# langchain-google-genai>=2.0.0    # Google Gemini
# langchain-aws>=0.2.0             # Amazon Bedrock
