# ═══════════════════════════════════════════════════════════════════════
# Cognitive Core — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════
# Copy to .env and fill in the values for your environment.
# Only LLM_PROVIDER and the matching provider credentials are required
# for real LLM execution. Everything else is optional or has defaults.
# ═══════════════════════════════════════════════════════════════════════

# ── LLM Provider ────────────────────────────────────────────────────
# Which provider to use: azure_foundry | azure | openai | google | bedrock
LLM_PROVIDER=azure_foundry

# Override the default model alias (optional)
# LLM_DEFAULT_MODEL=gpt-4o

# ── Azure AI Foundry ────────────────────────────────────────────────
AZURE_AI_FOUNDRY_ENDPOINT=https://<project>.services.ai.azure.com
AZURE_AI_FOUNDRY_KEY=<your-key>

# ── Azure OpenAI ────────────────────────────────────────────────────
# AZURE_OPENAI_ENDPOINT=https://<resource>.openai.azure.com/
# AZURE_OPENAI_API_KEY=<your-key>
# AZURE_OPENAI_API_VERSION=2024-12-01-preview

# ── OpenAI ──────────────────────────────────────────────────────────
# OPENAI_API_KEY=sk-...

# ── Google Gemini ───────────────────────────────────────────────────
# GOOGLE_API_KEY=AIza...

# ── Amazon Bedrock ──────────────────────────────────────────────────
# AWS_DEFAULT_REGION=us-east-1
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...

# ── MCP Tool Servers ────────────────────────────────────────────────
# Dev: stdio transport (runs server as subprocess)
# CLAIMS_MCP_CMD=python mcp_servers/claims_services.py

# Prod: HTTP transport (connect to running server)
# CLAIMS_MCP_URL=https://claims-mcp.internal/mcp

# ── Database ────────────────────────────────────────────────────────
# Default: SQLite in working directory (coordinator.db)
# Prod: Postgres
# DATABASE_URL=postgresql://user:pass@host:5432/cognitive_core

# ── Governance ──────────────────────────────────────────────────────
# CC_GOVERNANCE_DEFAULT_TIER=gate
# CC_MAX_DELEGATION_DEPTH=5
# CC_MAX_LOOP_COUNT=3
