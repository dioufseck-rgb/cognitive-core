You are performing a verification task.

CONTEXT:
{context}

SUBJECT TO VERIFY:
{subject}

INPUT DATA (reference):
{input}

RULES AND CRITERIA TO CHECK:
{rules}

{additional_instructions}

INSTRUCTIONS:
1. Read every rule in RULES AND CRITERIA TO CHECK. Count them. You must
   check ALL of them — not just the ones you expect to fail.
2. Use the SUBJECT TO VERIFY section as the primary data. If the subject
   contains specific values (e.g. "Policy status: active"), use those
   values directly. INPUT DATA provides additional reference.
3. For each rule, determine whether the subject data conforms or violates.
   Add EVERY rule to rules_checked regardless of whether it passed or failed.
   A rules_checked list that omits passing rules is incomplete and will be
   treated as low coverage by the quality gate.
4. For violations, describe what specifically fails and how severe it is.
5. Provide an overall conformance determination.

COMPLETENESS REQUIREMENT:
rules_checked must contain every rule from RULES AND CRITERIA TO CHECK.
If you check 6 rules, rules_checked must have 6 entries — not just the
ones with violations. The epistemic layer computes rule coverage as
rules_checked.length / total_rules. Omitting passing rules understates
coverage and may trigger unnecessary escalation.

CONFORMANCE LOGIC:
- conforms = true ONLY if ALL rules pass with zero violations.
- conforms = false if ANY rule has a violation.
- If a field's value clearly matches the rule's requirement, that rule PASSES.
  Example: if coverage_type is "liability,comprehensive" and the rule
  requires "liability", that rule PASSES because the string contains it.
- Do NOT invent requirements beyond what the rules explicitly state.
- Do NOT flag violations for rules clearly satisfied by the data.

Severity levels:
- critical: Must be fixed before proceeding, regulatory or safety risk
- major: Should be fixed, significant quality or compliance concern
- minor: Should be noted, low risk, can be addressed later

Respond with a JSON object matching this structure:
{{
  "conforms": true or false,
  "confidence": 0.0,
  "reasoning": "assessment citing specific evidence for each rule checked",
  "violations": [
    {{
      "rule": "which rule was violated",
      "description": "what the violation is, citing the specific text or value",
      "severity": "critical | major | minor",
      "location": "where in the input the violation occurs"
    }}
  ],
  "rules_checked": [
    "every rule that was checked — both passing and failing ones"
  ],
  "evidence_used": [
    {{"source": "where this evidence came from", "description": "the specific content checked"}}
  ],
  "evidence_missing": [
    {{"source": "where to find it", "description": "what it would clarify"}}
  ],
  "reasoning_quality": 0.0 to 1.0,
  "outcome_certainty": 0.0 to 1.0
}}

reasoning_quality: How rigorously was each rule applied?
  0.0 = rules not independently checked; 1.0 = each rule checked independently with specific citations.
  Report honestly — used by governance. Do not inflate.

outcome_certainty: How certain is the conformance determination?
  0.0 = rule applicability is genuinely ambiguous; 1.0 = clear conformance or clear violation with no ambiguity.

Respond ONLY with the JSON object. No other text.